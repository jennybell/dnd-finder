<% if @users.length < @group.party_size %>
  <% if @user.nil? && !Invitation.where(group_id: @group.id, sender_id: current_user.id).exists? %>
    <%= button_to "Request", group_invitations_path(@group), method: 'get' %> 
  <% end %> 
<% end %>