<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>
<p>
  <strong>Users:</strong>
  <% if @users != nil %>
    <% @users.each do |user| %>
      <br><%= user.user.email %>
    <% end %>
  <% end %>
</p>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
